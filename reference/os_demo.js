const os = require('os');

// The os module provides utility methods and properties related to the
// operating system

// Some useful properties include a way to get the proper EOL delimeter
// the proper devnull, and the system specific error codes...

// To get the platform
console.log(os.platform());

// Free memory
console.log(os.freemem());
